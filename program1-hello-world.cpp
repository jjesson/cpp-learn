// This is how to write a comment.

//This is how all programs should be started in C++, denoted by xxx color
#include <iostream>

int main()
{
  //We use std:: because it is a standard command in c++
  std:: cout<<"Hello World";
  //cout is the equivalent to a print statement.
  
  //We need to return a 0 because we started the program with int (integer)
  return 0;
}
